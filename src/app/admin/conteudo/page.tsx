import Link from 'next/link';import {routeKinds,kindLabels} from '@/modules/admin/fields';
export default function ContentHub(){return <><p className="eyebrow">Editor modular</p><h1 className="compact">Conteúdo</h1><p>O preview usa os mesmos componentes públicos. Salvar rascunho não publica automaticamente.</p><div className="grid">{Object.entries(routeKinds).map(([route,kind])=><article className="panel" key={route}><h2>{kindLabels[kind]}</h2><p>Gerencie registros, histórico, rascunhos e publicação.</p><Link href={`/admin/${route}`}>Abrir {kindLabels[kind].toLowerCase()}</Link></article>)}</div></>;}
