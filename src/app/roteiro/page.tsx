import Link from 'next/link';import {publicDataset,publicContent} from '@/modules/content/repository';import {Onboarding} from '@/modules/trips/Onboarding';import {trySanitizeId} from '@/core/security/sanitize';
export const dynamic='force-dynamic';export const metadata={title:'Planejar minha viagem',description:'Conte como você quer viver Serra Negra e monte um roteiro personalizado a partir das suas escolhas.',alternates:{canonical:'/roteiro'}};
function today(){return new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());}
export default async function Page({searchParams}:{searchParams:Promise<{interest?:string;resume?:string}>}){const categories=await publicContent('categories');const data=await publicDataset();const {interest:rawInterest,resume}=await searchParams;const interest=rawInterest?trySanitizeId(rawInterest,'Interesse'):null;return <><section className="section" style={{paddingTop:'1rem'}}><p className="eyebrow">Roteiro personalizado</p><h1>Monte uma viagem que combine com o seu ritmo.</h1><p className="lead">Informe datas, companhia, interesses e necessidades para construir uma primeira versão do seu caminho. Se preferir começar mais rápido, escolha um roteiro pronto e adapte depois.</p><div className="actions"><Link className="button" href="/roteiros">Ver roteiros prontos</Link></div></section><Onboarding guestPlaces={data.places.filter(p=>p.discoveryVisible!==false&&p.status!=='archived')} categories={categories} initialInterest={interest&&categories.some(c=>c.id===interest)?interest:undefined} defaultStart={today()} resume={resume==='1'}/></>;}
