import Link from 'next/link';import {publicDataset,publicContent} from '@/modules/content/repository';import {Onboarding} from '@/modules/trips/Onboarding';import {trySanitizeId} from '@/core/security/sanitize';
export const dynamic='force-dynamic';export const metadata={title:'Criar roteiro do zero',description:'Conte os detalhes da sua viagem para montar um roteiro personalizado do zero em Serra Negra.',alternates:{canonical:'/roteiro'}};
function today(){return new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());}
export default async function Page({searchParams}:{searchParams:Promise<{interest?:string;resume?:string}>}){const categories=await publicContent('categories');const data=await publicDataset();const {interest:rawInterest,resume}=await searchParams;const interest=rawInterest?trySanitizeId(rawInterest,'Interesse'):null;return <><section className="section" style={{paddingTop:'1rem'}}><p className="eyebrow">Criar do zero</p><h1>Monte uma viagem a partir das suas escolhas.</h1><p className="lead">Este é o caminho detalhado para quem prefere começar sem uma base pronta. Se quiser chegar mais rápido a uma primeira versão, escolha um roteiro curado e adapte depois.</p><div className="actions"><Link className="button" href="/roteiros">Ver roteiros prontos</Link></div></section><Onboarding guestPlaces={data.places.filter(p=>p.discoveryVisible!==false&&p.status!=='archived')} categories={categories} initialInterest={interest&&categories.some(c=>c.id===interest)?interest:undefined} defaultStart={today()} resume={resume==='1'}/></>;}
