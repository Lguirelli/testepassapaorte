import {getRepository} from "@/core/repository";export const dynamic="force-dynamic";export default async function Page(){const r=getRepository();const places=await r.list('places');return <div className="container section"><span className="eyebrow">Health</span><h1>Aplicação pronta para validação</h1><div className="success-state"><strong>OK</strong><div>Persistence: {r.mode} · entities places: {places.length} · demo mode: active</div></div></div>}
