import {notFound} from "next/navigation";import type {Place} from "@/core/domain/types";import {getRepository} from "@/core/repository";import {TripEditorClient} from "@/components/trip-editor-client";import {PageTracker} from "@/components/page-tracker";
export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{tripId:string}>}){const {tripId}=await params;const r=getRepository();const [bundle,places]=await Promise.all([r.getTripBundle(tripId),r.list<Place>('places',{publishedOnly:true})]);if(!bundle)notFound();return <div className="container section"><PageTracker payload={{route:`/viagens/${tripId}/roteiro`,tripId}}/><div className="section-head"><span className="eyebrow">Roteiro demo</span><h1>Sua viagem em blocos editáveis</h1><p>Itens fixos são preservados e cada alteração é local.</p></div><TripEditorClient initial={bundle} places={places.map(x=>x.data)}/></div>}
