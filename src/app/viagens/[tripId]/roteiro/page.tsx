import {notFound} from 'next/navigation';import Link from 'next/link';import {getTrip} from '@/modules/trips/repository';import {publicDataset,placeUrl} from '@/modules/content/repository';import {Weather,MockMap,EventList,labels} from '@/components/content';import {ItemControls} from '@/modules/trips/ItemControls';import {routesProvider} from '@/providers';import {minutes,timeLabel} from '@/modules/trips/engine';
export const dynamic='force-dynamic';export const metadata={title:'Roteiro demo'};
export default async function Page({params,searchParams}:{params:Promise<{tripId:string}>;searchParams:Promise<{day?:string}>}){if((await params).tripId!=='demo-trip-001')notFound();const row=await getTrip();const trip=row.data;const data=await publicDataset();const {day:date}=await searchParams;const day=trip.days.find(d=>d.date===date)||trip.days[0];if(!day)return <p>Sem dias planejados.</p>;const active=day.items.filter(i=>i.state!=='removed');return <><p className="eyebrow">Viagem de demonstração</p><h1 className="compact">Seu roteiro, dia a dia.</h1><p>{trip.trip.startsOn} a {trip.trip.endsOn} · usuário fictício · versão {row.version}</p><div className="actions"><Link className="button" href="/viagens/demo-trip-001/calendario">Ver calendário</Link><Link className="button" href="/meu-passaporte">Abrir Passaporte</Link><Link href="/roteiro">Rever perfil da viagem</Link></div><nav className="chips section" aria-label="Dias da viagem">{trip.days.map((d,i)=><Link className="button" aria-current={d.date===day.date?'page':undefined} key={d.date} href={`?day=${d.date}`}>Dia {i+1} · {d.date}</Link>)}</nav><div className="two"><div><Weather date={day.date}/><h2>Paradas de {day.date}</h2><div className="timeline">{active.map((item,index)=>{const p=data.places.find(p=>p.id===item.placeId);const previous=active[index-1];const route=previous?routesProvider.between(previous.placeId,item.placeId):null;const gap=previous?minutes(item.startsAt)-minutes(previous.startsAt)-previous.durationMinutes-(route?.minutes||0):0;const visit=trip.visits.some(v=>v.placeId===item.placeId&&v.occurredAt.slice(0,10)===day.date);return <div key={item.id}>{route&&<p className="muted">Deslocamento demo: {route.minutes} min · {route.distanceKm} km{gap>0?` · ${gap} min livres`:gap<0?' · Atenção: conflito de horários':''}</p>}<article className="card" data-testid={`trip-${item.id}`}><span className="eyebrow">{item.startsAt} a {timeLabel(minutes(item.startsAt)+item.durationMinutes)}</span><h3>{p?<Link href={placeUrl(p)}>{p.name}</Link>:'Lugar indisponível'}</h3><p>{p?.shortDescription}</p><div className="actions"><span className="badge">{labels[item.state]||item.state}</span><span className="badge">{visit?'Presença registrada demo':'Sem evidência registrada'}</span></div><small>{item.source==='recommended_by_engine'?'Sugestão demo':item.source==='added_by_user'?'Adicionado por você':'Escolha manual preservada'}</small><ItemControls item={item} trip={trip} places={data.places} version={row.version}/></article></div>;})}</div>{!active.length&&<p className="empty">Dia livre. Adicione uma parada.</p>}<section className="panel"><ItemControls trip={trip} places={data.places} version={row.version}/></section>{day.items.some(i=>i.state==='removed')&&<section className="section"><h2>Paradas removidas</h2>{day.items.filter(i=>i.state==='removed').map(i=><article className="panel" key={i.id}><h3>{data.places.find(p=>p.id===i.placeId)?.name}</h3><span className="badge">Removido</span><ItemControls item={i} trip={trip} places={data.places} version={row.version}/></article>)}</section>}</div><aside className="stack" style={{alignContent:'start'}}><MockMap places={active.flatMap(i=>{const p=data.places.find(p=>p.id===i.placeId);return p?[p]:[];})}/><EventList events={data.events.filter(e=>e.startsAt?.slice(0,10)===day.date)}/><section className="panel"><h2>Seu planejamento</h2><p>{active.length} paradas · {active.reduce((s,i)=>s+i.durationMinutes,0)} min em atividades.</p><p>Sem evidência registrada não significa que a visita não aconteceu.</p></section></aside></div></>;}
