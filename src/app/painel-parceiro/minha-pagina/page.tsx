import Link from 'next/link';import {currentActor} from '@/core/auth/session';import {partnerContext} from '@/modules/partners/service';import {PartnerEditForm,PartnerRequestForm} from '@/modules/partners/PartnerForms';
export const dynamic='force-dynamic';export default async function MyPage(){const actor=(await currentActor())!;const {partner,place,media}=await partnerContext(actor);return <><div className="section-head"><div><p className="eyebrow">Minha Página</p><h1 className="compact">Mantenha sua presença atualizada.</h1></div><Link className="button" href={`/parceiros/${place.slug}`} target="_blank">Visualizar página pública</Link></div><div className="two"><PartnerEditForm partner={partner} place={place} imageSrc={media?.src||''}/><PartnerRequestForm/></div></>;}
