'use client';
import {useActionState} from 'react';
import {loginAction} from './actions';
export function LoginForm({next}:{next:string}){const[state,action,pending]=useActionState(loginAction,{message:''});return <section className="auth-shell panel"><p className="eyebrow">Acesso seguro</p><h1 className="compact">Entre no Passaporte</h1><p>O acesso persistente é necessário apenas para viagens, Passaporte, área do parceiro e Admin.</p><form action={action} className="stack"><input type="hidden" name="next" value={next}/><label className="field">E-mail<input name="email" type="email" autoComplete="username" required/></label><label className="field">Senha<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary" disabled={pending}>{pending?'Entrando…':'Entrar'}</button>{state.message&&<p role="alert">{state.message}</p>}</form><details><summary>Ambiente local</summary><p>Credenciais de desenvolvimento são configuradas em <code>.env.local</code> e nunca são usadas como autorização enviada pelo navegador.</p></details></section>}
