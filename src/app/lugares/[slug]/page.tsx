import {notFound,redirect} from 'next/navigation';import {publicDataset,placeUrl} from '@/modules/content/repository';import {getCurrentTrip} from '@/modules/trips/repository';import {EntityPage} from '@/components/EntityPage';import {trySanitizeSlug} from '@/core/security/sanitize';
export const dynamic='force-dynamic';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const slug=trySanitizeSlug((await params).slug);if(!slug)return{title:'Lugar indisponível'};const p=(await publicDataset()).places.find(p=>p.slug===slug);return{title:p?.name||'Lugar indisponível',description:p?.shortDescription,alternates:{canonical:`/lugares/${slug}`}};}
export default async function Page({params}:{params:Promise<{slug:string}>}){const slug=trySanitizeSlug((await params).slug);if(!slug)notFound();const data=await publicDataset();const place=data.places.find(p=>p.slug===slug&&p.discoveryVisible!==false);if(!place)notFound();if(place.commercialRelation==='partner')redirect(placeUrl(place));const trip=await getCurrentTrip();return <EntityPage place={place} data={data} trip={trip?.data}/>;}
