import {currentActor} from '@/core/auth/session';import {sanitizeId} from '@/core/security/sanitize';import {getTripById} from '@/modules/trips/repository';import {publicDataset} from '@/modules/content/repository';import {passportSummary} from '@/modules/passport/model';import {analyticsProvider} from '@/modules/tracking/service';
const sizes={story:[1080,1920],feed:[1080,1350],square:[1080,1080]} as const;
function esc(s:string){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]!));}
export async function GET(req:Request,{params}:{params:Promise<{tripId:string}>}){const actor=await currentActor();if(!actor)return new Response('Não autorizado',{status:401});let tripId:string;try{tripId=sanitizeId((await params).tripId,'Viagem');}catch{return new Response('Não encontrado',{status:404});}let row;try{row=await getTripById(tripId,actor);}catch{return new Response('Não encontrado',{status:404});}const url=new URL(req.url);const rawFormat=url.searchParams.get('format')||'story';if(!Object.prototype.hasOwnProperty.call(sizes,rawFormat))return new Response('Formato inválido',{status:400});const format=rawFormat as keyof typeof sizes;const [w,h]=sizes[format];const privateMode=url.searchParams.get('privacy')==='compact';const data=await publicDataset();const summary=passportSummary(row.data,data.places);const names=privateMode?[]:summary.recordedIds.slice(0,6).map(id=>data.places.find(p=>p.id===id)?.name).filter(Boolean) as string[];const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}"><rect width="100%" height="100%" fill="#F0EDE5"/><rect x="70" y="70" width="${w-140}" height="${h-140}" rx="36" fill="none" stroke="#403D3F" stroke-width="3"/><text x="100" y="180" font-family="serif" font-size="38" letter-spacing="6" fill="#403D3F">PASSAPORTE SERRA NEGRA</text><text x="100" y="300" font-family="serif" font-size="72" font-weight="700" fill="#161618">Minha história em</text><text x="100" y="385" font-family="serif" font-size="72" font-weight="700" fill="#161618">Serra Negra</text><text x="100" y="500" font-family="sans-serif" font-size="30" fill="#403D3F">${privateMode?'Datas ocultas':`${esc(row.data.trip.startsOn)} → ${esc(row.data.trip.endsOn)}`}</text><text x="100" y="585" font-family="sans-serif" font-size="30" fill="#403D3F">${summary.recordedIds.length} lugares registrados · ${summary.returns} reencontros</text>${names.map((n,i)=>`<g transform="translate(${100+(i%2)*((w-220)/2)},${720+Math.floor(i/2)*180}) rotate(${i%2?2:-2})"><rect width="${(w-260)/2}" height="130" rx="65" fill="none" stroke="#4E0000" stroke-width="5"/><text x="30" y="58" font-family="sans-serif" font-size="24" font-weight="700" fill="#4E0000">${esc(n)}</text><text x="30" y="94" font-family="sans-serif" font-size="18" fill="#4E0000">VISITA REGISTRADA</text></g>`).join('')}<text x="100" y="${h-120}" font-family="sans-serif" font-size="22" fill="#5E5A50">Descubra Serra Negra do seu jeito.</text></svg>`;await analyticsProvider.record('PASSPORT_SHARED',{format,privacy:privateMode?'compact':'standard'},actor);return new Response(svg,{headers:{'Content-Type':'image/svg+xml; charset=utf-8','Content-Disposition':`inline; filename="passaporte-serra-negra-${format}.svg"`,'Cache-Control':'private, no-store','Content-Security-Policy':"default-src 'none'; style-src 'unsafe-inline'"}});}
