import type {MetadataRoute} from 'next';import {publicDataset,placeUrl} from '@/modules/content/repository';import {siteOrigin} from '@/core/security/origin';
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=siteOrigin();const data=await publicDataset();const staticRoutes=['/','/explorar','/mapa','/pontos-turisticos','/parceiros','/roteiro','/privacidade','/termos','/cookies','/acessibilidade'];return [...staticRoutes.map(url=>({url:`${base}${url}`,changeFrequency:'weekly' as const,priority:url==='/'?1:.7})),...data.places.filter(p=>p.discoveryVisible!==false).map(p=>({url:`${base}${placeUrl(p)}`,changeFrequency:'weekly' as const,priority:.8}))];}
