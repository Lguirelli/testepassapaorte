export type Party='couple'|'family'|'friends'|'solo';
export type Pace='slow'|'balanced'|'active';
export type Transport='car'|'walk'|'taxi_app'|'unknown';
export type TripItemSource='recommended_by_engine'|'added_by_user'|'changed_by_user'|'fixed';
export type TripItemState='planned'|'fixed'|'moved'|'removed';
export type TripItem={id:string;placeId:string;startsAt:string;durationMinutes:number;source:TripItemSource;state:TripItemState;previousState?:TripItemState;sortOrder?:number};
export type TripDay={id?:string;date:string;items:TripItem[]};
export type Visit={id:string;placeId:string;occurredAt:string;evidence:string;visitNumber:number;isReturn:boolean;outsidePlannedRoute?:boolean};
export type TripWeather={date:string;condition?:string;temperatureC?:number;rainProbability?:number;sourceId?:string};
export type TripProfile={startsOn:string;endsOn:string;party:Party;interests:string[];intentions:string[];pace:Pace;transport:Transport;needs:string[]};
export type TripData={trip:{id:string;synthetic:boolean;cityId:string;startsOn:string;endsOn:string;party:Party;pace:Pace;transport:Transport;interests:string[];intentions:string[];needs:string[]};days:TripDay[];visits:Visit[];weather:TripWeather[]};
export type TripRecord={id:string;owner:string;version:number;data:TripData};
