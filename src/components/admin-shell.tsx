import Link from "next/link";import type {ReactNode} from "react";import {Icon} from "@/design-system/icons";import {adminSections} from "@/modules/admin/config";
export function AdminShell({children}:{children:ReactNode}){return <div className="admin-shell"><aside className="admin-nav" aria-label="Admin"> <Link href="/admin"><Icon name="admin-dashboard" size="sm"/>Visão geral</Link>{Object.entries(adminSections).map(([slug,s])=><Link key={slug} href={`/admin/${slug}`}><Icon name={slug==='fontes'?'admin-fontes':'admin-conteudo-cms'} size="sm"/>{s.label}</Link>)}</aside><div className="admin-main">{children}</div></div>}
