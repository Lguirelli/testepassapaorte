'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
export function ConsentBanner(){const[visible,setVisible]=useState(false);const[error,setError]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{setVisible(!document.cookie.split('; ').some(c=>c.startsWith('psn_analytics=')));},[]);if(!visible)return null;async function save(status:'granted'|'denied'){setSaving(true);setError('');try{const response=await fetch('/api/consent',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});if(!response.ok)throw new Error();setVisible(false);}catch{setError('Não foi possível salvar sua preferência. Tente novamente.');}finally{setSaving(false);}}return <aside className="consent-banner" role="dialog" aria-label="Preferências de privacidade"><div><strong>Privacidade primeiro</strong><p>Cookies essenciais mantêm a sessão. Analytics opcional só é ativado com sua escolha e não usa rastreamento GPS contínuo. <Link href="/cookies">Ver detalhes</Link>.</p>{error&&<p role="alert">{error}</p>}</div><div className="actions"><button disabled={saving} onClick={()=>save('denied')}>Somente essenciais</button><button disabled={saving} className="primary" onClick={()=>save('granted')}>{saving?'Salvando…':'Permitir analytics'}</button></div></aside>;}
