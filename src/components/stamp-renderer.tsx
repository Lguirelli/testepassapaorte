import type {Place,Visit} from "@/core/domain/types";import {TourismStamp} from "@/features/stamps/react";
export function StampRenderer({visit,place,category}:{visit:Visit;place:Place;category:string}){return <TourismStamp data={{partnerId:place.id,partnerName:place.name,category,visitId:visit.id,visitDate:visit.occurredAt.slice(0,10),visitNumber:visit.isReturn?2:1,city:'Serra Negra',state:'SP',status:visit.isReturn?'return':'first_visit',seed:visit.stampSeed}} options={{size:230,textureLevel:.58,showLocation:true,showVisitNumber:true,animated:true,variationMode:'visit'}}/>}
