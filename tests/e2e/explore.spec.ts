import {test,expect} from '@playwright/test';
test('busca, parceiro, navegação e retorno preservam estado',async({page})=>{await page.goto('/explorar');const input=page.getByLabel('Busca');await input.fill('Café');await input.blur();await page.getByRole('button',{name:'Parceiros'}).click();await expect(page.getByText('Café Neblina Alta')).toBeVisible();await page.getByRole('link',{name:'Abrir Café Neblina Alta'}).click();await expect(page).toHaveURL(/\/parceiros\/cafe-neblina-alta/);await page.goBack();await expect(input).toHaveValue('Café');await expect(page.getByRole('button',{name:'Parceiros'})).toHaveAttribute('aria-pressed','true');await expect(page.getByText('Café Neblina Alta')).toBeVisible()});
