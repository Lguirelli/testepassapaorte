import {test,expect} from '@playwright/test';
test('G6 calendar views and shared itinerary data',async({page},info)=>{await page.goto('/viagens/demo-trip-001/roteiro');const text=await page.getByTestId('trip-trip-item-002').innerText();const time=text.match(/\d{2}:\d{2}/)?.[0];await page.getByRole('link',{name:'Ver calendário'}).click();await expect(page.getByTestId('calendar-trip-item-002')).toContainText(time||'11:00');await expect(page.getByText('Presença registrada demo').first()).toBeVisible();await page.getByRole('navigation',{name:'Visualização do calendário'}).getByRole('link',{name:'Semana',exact:true}).click();await expect(page.getByTestId('agenda-2026-09-13')).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();await page.screenshot({path:`artifacts/playwright/${info.project.name}/05-semana.png`,fullPage:true});await page.getByRole('navigation',{name:'Visualização do calendário'}).getByRole('link',{name:'Mês',exact:true}).click();await expect(page.getByRole('heading',{name:'setembro de 2026',exact:true})).toBeVisible();await page.screenshot({path:`artifacts/playwright/${info.project.name}/05-mes.png`,fullPage:true});});
