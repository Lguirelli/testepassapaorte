import {test,expect} from '@playwright/test';
test('partner is confined to its own portal',async({page},info)=>{await page.goto('/login?next=%2Fpainel-parceiro');await page.getByLabel('E-mail').fill('parceiro@passaporte.local');await page.getByLabel('Senha').fill('parceiro-local');await page.getByRole('button',{name:'Entrar'}).click();await expect(page).toHaveURL(/painel-parceiro/);await expect(page.getByText(/Governança/i)).toBeVisible();await page.goto('/admin');await expect(page).toHaveURL('http://localhost:4173/');await page.screenshot({path:`docs/validation/${info.project.name}-partner.png`,fullPage:true});});
