import {test,expect} from '@playwright/test';
async function login(page:import('@playwright/test').Page){await page.goto('/admin');if(await page.getByRole('button',{name:'Entrar no Admin demo'}).isVisible())await page.getByRole('button',{name:'Entrar no Admin demo'}).click();await expect(page.getByRole('heading',{name:'Admin operacional',level:1})).toBeVisible();}
test('G4 draft preview publish and audit',async({page},info)=>{await page.goto('/parceiros/cafe-neblina-alta');const original=await page.getByTestId('public-description').innerText();await login(page);await page.goto('/admin/lugares/place-cafe-neblina');const value=`Descrição de validação ${info.project.name} ${Date.now()}.`;await page.getByLabel('Descrição curta',{exact:true}).fill(value);await page.getByRole('button',{name:'Salvar rascunho'}).click();await expect(page.getByText('Rascunho salvo. O público permanece na versão publicada.',{exact:true})).toBeVisible();await page.goto('/parceiros/cafe-neblina-alta');await expect(page.getByTestId('public-description')).toHaveText(original);await page.goto('/admin/lugares/place-cafe-neblina/preview');await expect(page.getByTestId('public-description')).toHaveText(value);await page.screenshot({path:`artifacts/playwright/${info.project.name}/03-preview.png`,fullPage:true});await page.getByRole('link',{name:'Voltar ao editor'}).click();await page.getByRole('button',{name:'Publicar',exact:true}).click();await expect(page.getByText('Conteúdo publicado.',{exact:true})).toBeVisible();await page.reload();await expect(page.getByText(/^publish ·/).first()).toBeVisible();await page.goto('/parceiros/cafe-neblina-alta');await expect(page.getByTestId('public-description')).toHaveText(value);await page.goto('/admin/lugares/place-cafe-neblina');await page.getByLabel('Descrição curta',{exact:true}).fill(original);await page.getByRole('button',{name:'Salvar rascunho'}).click();await expect(page.getByText('Rascunho salvo. O público permanece na versão publicada.',{exact:true})).toBeVisible();await page.getByRole('button',{name:'Publicar',exact:true}).click();await expect(page.getByText('Conteúdo publicado.',{exact:true})).toBeVisible();});
for(const kind of ['lugares','experiencias','parceiros','eventos','categorias','fontes'])test(`G4 list ${kind}`,async({page},info)=>{await login(page);await page.goto(`/admin/${kind}`);await expect(page.getByRole('link',{name:'Criar registro'})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();await page.screenshot({path:`artifacts/playwright/${info.project.name}/03-${kind}.png`,fullPage:true});});
test('G4 anonymous cannot read editor or preview',async({page})=>{await page.goto('/admin/lugares/place-cafe-neblina/preview');await expect(page.getByRole('button',{name:'Entrar no Admin demo'})).toBeVisible();});
