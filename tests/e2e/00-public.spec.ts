import {test,expect} from '@playwright/test';import AxeBuilder from '@axe-core/playwright';
test('home, discovery and legal shell are usable',async({page},info)=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Comece pela curiosidade/i})).toBeVisible();await page.getByRole('link',{name:/Explorar/i}).first().click();await expect(page).toHaveURL(/\/explorar/);await expect(page.locator('[data-testid^="place-card-"]').first()).toBeVisible();await page.goto('/privacidade');await expect(page.getByRole('heading',{name:/Privacidade/i})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();const result=await new AxeBuilder({page}).analyze();expect(result.violations.filter(v=>['critical','serious'].includes(v.impact||''))).toEqual([]);await page.screenshot({path:`docs/validation/${info.project.name}-public.png`,fullPage:true});});
