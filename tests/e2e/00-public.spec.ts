import {test,expect} from '@playwright/test';import AxeBuilder from '@axe-core/playwright';
test('home, ready routes, discovery and legal shell are usable',async({page},info)=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Escolha um ponto de partida/i})).toBeVisible();await page.getByRole('link',{name:'Explorar roteiros',exact:true}).first().click();await expect(page).toHaveURL(/\/roteiros/);await expect(page.getByRole('heading',{name:/Comece com uma direção/i})).toBeVisible();await page.goto('/explorar');await expect(page.locator('[data-testid^="place-card-"]').first()).toBeVisible();await page.goto('/privacidade');await expect(page.getByRole('heading',{name:/Privacidade/i})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();const result=await new AxeBuilder({page}).analyze();expect(result.violations.filter(v=>['critical','serious'].includes(v.impact||''))).toEqual([]);await page.screenshot({path:`docs/validation/${info.project.name}-public.png`,fullPage:true});});