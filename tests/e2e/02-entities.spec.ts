import {test,expect} from '@playwright/test';import AxeBuilder from '@axe-core/playwright';
for(const [kind,slug] of [['lugares','mirante-vale-das-araucarias'],['parceiros','cafe-neblina-alta']])test(`G3 ${kind}`,async({page},info)=>{const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`/${kind}/${slug}`);await expect(page.getByRole('heading',{name:kind==='lugares'?'Mirante Vale das Araucárias':'Café Neblina Alta',level:1})).toBeVisible();await expect(page.getByRole('button',{name:'Adicionar ao roteiro'})).toBeVisible();expect(await page.locator('a[href*="example.invalid"]').count()).toBe(0);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();expect((await new AxeBuilder({page}).analyze()).violations).toEqual([]);await page.screenshot({path:`artifacts/playwright/${info.project.name}/02-${kind}.png`,fullPage:true});expect(errors).toEqual([]);});
