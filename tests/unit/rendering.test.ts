import {test} from 'node:test';import assert from 'node:assert/strict';import {formDataToContent} from '../../src/modules/admin/fields';
test('empty optional select values are absent and unknown form fields are discarded',()=>{const form=new FormData();form.set('name','Categoria de teste');form.set('slug','categoria-de-teste');form.set('sql','DROP TABLE content');form.set('enabled','true');form.set('sortOrder','10');const result=formDataToContent('categories',form,{id:'test'});assert.equal(result.name,'Categoria de teste');assert.equal('sql' in result,false);assert.equal(result.synthetic,true);});
