import test from 'node:test';import assert from 'node:assert/strict';import {pageKindForPath} from '../../src/core/routing/page-kind';import {MAIN_NAV,ROUTES} from '../../src/core/routing/routes';
test('canonical routes resolve by responsibility',()=>{assert.equal(pageKindForPath('/'),'home');assert.equal(pageKindForPath('/explorar'),'explore');assert.equal(pageKindForPath('/roteiros'),'ready-routes');assert.equal(pageKindForPath('/roteiros/fim-de-semana-a-dois'),'ready-route-detail');assert.equal(pageKindForPath('/roteiro'),'onboarding');assert.equal(pageKindForPath('/parceiros'),'partner-acquisition');assert.equal(pageKindForPath('/lugares/mirante-alto-da-serra'),'tourism-detail');assert.equal(pageKindForPath('/viagens/trip-123/roteiro'),'trip-route');assert.equal(pageKindForPath('/viagens/trip-123/calendario'),'calendar');assert.equal(pageKindForPath('/meu-passaporte'),'passport');assert.equal(pageKindForPath('/admin/lugares/place-1'),'admin');});
test('main navigation routes are unique',()=>{const hrefs=MAIN_NAV.map(i=>i.href);assert.equal(new Set(hrefs).size,hrefs.length);assert.equal(ROUTES.explore,'/explorar');assert.equal(ROUTES.readyRoutes,'/roteiros');assert.equal(ROUTES.tripBuilder,'/roteiro');assert.equal(ROUTES.partnerProgram,'/parceiros');});