import test from 'node:test';import assert from 'node:assert/strict';import {itemEvidence} from '../../src/modules/trips/evidence';import type {TripData} from '../../src/modules/trips/types';
const item={id:'i',placeId:'p',startsAt:'10:00',durationMinutes:60,source:'recommended_by_engine' as const,state:'planned' as const};const trip:TripData={trip:{id:'t',synthetic:false,cityId:'serra-negra-sp',startsOn:'2026-09-12',endsOn:'2026-09-12',party:'solo',pace:'balanced',transport:'walk',interests:['x'],intentions:['classics'],needs:[]},days:[{date:'2026-09-12',items:[item]}],visits:[{id:'v',placeId:'p',occurredAt:'2026-09-12T11:00:00-03:00',evidence:'qr',visitNumber:1,isReturn:false}],weather:[]};
test('evidence is factual by place and date',()=>{assert.equal(itemEvidence(trip,item,'2026-09-12'),'Presença registrada');assert.equal(itemEvidence(trip,item,'2026-09-13'),'Sem evidência registrada');});
