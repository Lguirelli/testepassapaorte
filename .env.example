# Local demonstration only. External credentials are not required.
DATABASE_URL=postgres://validation:validation@localhost:5432/passaporte
DB_MODE=postgres
AUTH_MODE=mock
MAPS_MODE=mock
ROUTES_MODE=mock
WEATHER_MODE=mock
STORAGE_MODE=local
ANALYTICS_MODE=local
# Required even for a production build of this demonstration:
ALLOW_DEMO=true
# Set only for isolated, explicitly disclosed fallback without Docker:
# DB_MODE=pglite
# PGLITE_PATH=.data/pglite
